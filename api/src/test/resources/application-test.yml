spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/NEWS_PLATFORM
    username: news_user
    password: news_pass
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  liquibase:
    enabled: false

feign:
  user-service:
    url: http://localhost:8085
  comment-service:
    url: http://localhost:8082

server:
  port: 8081
